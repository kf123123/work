# 上位机 UI 设计修改说明

## 修改概览

本次修改针对 `work/code/` 下的两个文件：`main_window.py`（界面布局）和 `main.py`（样式与逻辑）。

---

## 修改一：布局重构（main_window.py）

### 原先的问题
- 所有控件用 **绝对定位**（setGeometry），无法自适应缩放
- 没有分组容器，视觉散乱
- 控制按钮一字排开，不符合遥控操作直觉
- 只有 4 个方向（前进/后退/左转/右转）

### 修改内容

| 项目 | 原先 | 修改后 |
|------|------|--------|
| 布局方式 | setGeometry 绝对定位 | QHBoxLayout + QVBoxLayout + QGridLayout |
| 窗口缩放 | 不支持 | 支持拉伸，左右比例 7:3 |
| 控件分组 | 无 | QGroupBox 包裹视频、控制、状态、地图 |
| 控制按钮 | 4方向平铺 | **8方向 D-pad 十字布局**（含左上/右上/左下/右下） |
| 速度滑块 | 独立放置 | 整合到控制面板，带实时数值显示 |
| 键盘提示 | 无 | 改为完整 9 键提示（W Q E / A S D / Z X C / 空格） |

### 布局结构

```
+------------------------------------------------------+
|  [■ 视频画面]              |  [■ 车辆状态]           |
|  +------------------------+ |  +------------------+  |
|  |   QLabel (video feed)  | |  |  参数     数值    |  |
|  |   640x480              | |  |  运行状态 停止    |  |
|  +------------------------+ |  |  当前速度 150     |  |
|                             |  |  视频帧率 0 FPS   |  |
|  [■ 车辆控制]               |  +------------------+  |
|  +----------------------+   |  [■ 定位地图]           |
|  |  [↖左上][↑前进][↗右上] |  |  +------------------+  |
|  |  [←左转][■停止][→右转] |  |  |  QGraphicsView   |  |
|  |  [↙左下][↓后退][↘右下] |  |  |  小车红点定位     |  |
|  +----------------------+   |  +------------------+  |
|      速度: [===] 150        |                        |
|      WASD / 空格            |                        |
+------------------------------------------------------+
```

---

## 修改二：配色更换（main.py → TEAL_STYLE）

### 原先配色（蓝色调）
| 元素 | 色值 |
|------|------|
| 背景 | #0f172a |
| 面板 | #1e293b |
| 强调色 | #38bdf8（天蓝） |

### 新配色（青绿色调）
| 元素 | 色值 | 说明 |
|------|------|------|
| 背景 | #111827 | 深灰底 |
| 面板 | #1f2937 | 暖灰卡片 |
| 标题强调色 | #2dd4bf | 青绿 |
| 滑块手柄 | #2dd4bf | 青绿 |
| 速度数值 | #2dd4bf | 青绿 |

### 按钮配色分层
- **前进** → 绿色（#065f46）
- **后退** → 橙色（#7c2d12）
- **左转/右转** → 蓝色（#1e3a5f）
- **左上/右上** → 青色（#155e75）
- **左下/右下** → 紫色（#4c1d95）
- **停止** → 红色（#7f1d1d）

每个按钮都有 hover 亮色和 pressed 按压反馈。

---

## 修改三：8方向控制（main.py）

### 新增通信协议 T 值

| 方向 | 常量名 | T值 | 按键 |
|------|--------|-----|------|
| 停止 | STOP | 115 | 空格 |
| 前进 | FORWARD | 116 | W |
| 后退 | BACKWARD | 117 | S |
| 左转 | LEFT | 118 | A |
| 右转 | RIGHT | 119 | D |
| 左上 | FWD_LEFT | 120 | Q |
| 右上 | FWD_RIGHT | 121 | E |
| 左下 | BWD_LEFT | 122 | Z |
| 右下 | BWD_RIGHT | 123 | C |

> 注：120-123 为新增指令，下位机（K230）需要添加对应处理逻辑。

### 键盘布局（类 WASD 游戏操作）

```
      Q  W  E
      A 空格 D
      Z  S  C
```

---

## 修改四：速度即时生效（main.py）

原先速度滑块只改变界面数值，实际速度在点击按钮时才生效。现在：

1. 拖动滑块 → `on_speed_changed()` 被触发
2. 更新显示的数值
3. **如果车辆当前正在运动**，立即用新速度重发当前指令
4. 控制台输出：`速度已更新为 180，即时重发指令 T=116`

这样驾驶员可以一边按住前进、一边拖动滑块调整速度，无需松手重按。

---

## 修改五：车辆状态面板（main.py）

右侧状态表格现在实时显示三项数据：

| 参数 | 数值 | 说明 |
|------|------|------|
| **运行状态** | 前进中 / 后退中 / 左转中 / 停止 ... | 根据当前 T 值自动变化，运动中为青色，停止为灰色 |
| **当前速度** | 150 | 跟随滑块实时变化 |
| **视频帧率** | 30 FPS | 由 VideoReceiveThread 每 1 秒统计一次 |

FPS 统计在视频接收线程内部完成：每解码一帧计数 +1，每秒发射一次 `fps_signal`。

---

## 不影响的功能

- [x] UDP 视频分片接收与显示
- [x] TCP 定位数据接收与解析（T=200）
- [x] 小地图红点定位（400x300 网格，红点偏移追踪）
- [x] 鼠标点击按钮控制
- [x] WASD + 空格键盘控制（新增 Q/E/Z/C 对角线）
- [x] TCP 断线重连与资源清理

---

## 快速调参指南

| 想改什么 | 在哪里改 |
|----------|----------|
| 主题色（青绿→别的色） | main.py 中 `TEAL_STYLE` 内所有 `#2dd4bf` / `#14b8a6` |
| 按钮颜色 | main.py 中对应 `QPushButton#btn_xxx` 的样式块 |
| 面板背景色 | main.py → `QGroupBox` 的 `background-color` |
| 指令 T 值 | main.py 顶部的 `CMD` 字典 |
| FPS 统计间隔 | main.py → `VideoReceiveThread.run()` 中的 `if elapsed >= 1.0` 改数值 |
| D-pad 按钮大小 | main_window.py → 各按钮的 `setMinimumSize` |
| 速度范围 | main_window.py → `speedviewer.setMaximum(200)` |

---

## 运行方式

```bash
cd work/code
python main.py
```
